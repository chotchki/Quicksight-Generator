# PLAN — Phase K: persona-driven layout work

## AR Exceptions redesign + AML app

**Audience.** Two unrelated persona splits surfaced from the Training Story conversations and the post-Phase-G/H walkthrough load:

- **GL Reconciliation / Accounting Operations** (existing AR persona) — the AR Exceptions tab has accumulated 3 rollups + 14 per-check blocks + aging bars on a single sheet. User flagged 2026-04-19 as density-overloaded. Fix: split by *workflow*, not by persona — every check serves the same persona; the cut is acute drill-now vs trend analysis.
- **AML Team** (new, not currently served) — three known question shapes that all need their own surface. Different cadence (potentially intraday) and a different visual repertoire from AR (statistical / graph / fanout, not double-entry math). Lives in a new `aml_recon` app, mirroring the existing PR + AR pattern.

**Explicitly NOT in Phase K:**

- *Fraud as a separate surface.* Fraud team is a stakeholder of the existing limit-breach check (and any similar limit-style checks that emerge); not a separate persona/tab/app. When K.1 lands, limit-breach becomes one of the rows in the unified Today's Exceptions table — Fraud just reads that.
- *PR pipeline tab restructure.* The shared-base model exposes Sales / Settlements / Payments as values of `transfer_type`, but the current per-step tab structure works for Merchant Support's workflow. Re-evaluate later if walkthroughs surface friction.
- *Bank Statement Reconciliation tab.* Already exists as the Daily Statement sheet (`ar-gs-daily-statement`); the AR-vs-PDF comparison is a manual eyeball workflow, no new dashboard surface needed.

**Architectural note.** Surface area expansion is expected — Phase K adds Today's Exceptions + Trends sheets to AR (split from existing Exceptions), plus a new `aml_recon` app. Tag-based cleanup, idempotent deploy, and the per-app analyses pattern already support this without rework.

## K.1 — AR Exceptions workflow split

Goal: replace the single dense Exceptions tab with two sheets — *Today's Exceptions* (the 9am scan, unified table) and *Exceptions Trends* (the weekly/monthly look, rollups + aging + per-check trend lines).

- [x] **K.1.0 — Pin the unified-exceptions dataset shape.** Decision is about the projection of the new `ar_unified_exceptions` dataset/view that UNION ALLs the 14 existing exception views — **the base `transactions` + `daily_balances` tables don't change in Phase K**. Two shapes on the table: wide schema with NULLs (every check's specific column present, NULLed for non-applicable rows) vs JSON `details` column (per-check specifics in JSON, harmonized columns first-class). **Locked: wide+NULL** (2026-04-20) — QuickSight handles NULLs in tables fine, the projection stays grep-able, and the contract test can lock the column list.
- [ ] **K.1.1 — `ar_unified_exceptions` dataset.** New dataset SQL that UNION ALLs across the 14 exception views, adds a `check_type` discriminator column and a computed `severity` column (`drift` / `overdraft` → red, `expected-zero` → orange, `limit-breach` → amber, others → yellow). Harmonize common columns (account_id, account_name, account_type, posted_at, balance_date, days_outstanding, aging_bucket). Declare a `DatasetContract`; lock with the existing contract test pattern.
- [ ] **K.1.2 — `Today's Exceptions` sheet.** Replace the existing Exceptions sheet's per-check blocks. Layout: KPI strip at top (14 count tiles, severity-colored, click-to-scope) → filter controls (check_type, account, age bucket, transfer_type, origin) → ONE unified table sorted by severity then aging. Drill: row → AR Transactions sheet scoped to `transfer_id`.
- [ ] **K.1.3 — `Exceptions Trends` sheet.** New sheet. Move the 3 cross-check rollups here (expected-zero EOD, two-sided post-mismatch, balance drift timelines). Add a stacked aging chart (5 buckets × 14 check types) and per-check trend lines (count over time).
- [ ] **K.1.4 — Drop the legacy per-check blocks.** Once Today's Exceptions + Trends are wired and tested, remove the 14 per-check KPI+table+aging blocks from the analysis builder and their generation paths if no longer referenced. Tag-based cleanup picks up any orphaned datasets on next deploy.
- [ ] **K.1.5 — Browser e2e for the new sheets.** Add `tests/e2e/test_ar_todays_exceptions.py` and `test_ar_exceptions_trends.py`: KPI tile click filters the unified table; severity sort works; drill to Transactions scopes correctly; trends sheet renders rollups + timelines.
- [ ] **K.1.5a — Triage + fix lingering e2e failures from the K.1 deploy.** The K.1 deploy run surfaced 25 e2e failures that don't block K.1 structurally but shouldn't linger:
    - [ ] **K.1.5a.1 — Reseed the demo DB (`quicksight-gen demo apply --all`).** Likely fixes most of them. Five AR data-side tests (`test_ar_daily_statement::TestDailyStatementScenarioCoverage` ×2, `test_ar_kpi_semantics::TestPlantedRowsSurface` ×3) and ~10 PR date-window / mutual-filter tests use `date.today() - days_ago`; the seed pins planted dates to the day it was generated, so a stale seed shifts every assertion off by N days. Reseed against today, re-run e2e, re-categorize.
    - [ ] **K.1.5a.2 — Redeploy PR (`quicksight-gen deploy payment-recon`).** The K.1.5 deploy of `account-recon` ran a delete-then-create on every dataset (PR + AR) but only redeployed the AR analysis/dashboard. Dataset IDs are deterministic so the ARNs survive, but if PR's analysis cached anything off the prior dataset state, this refreshes it. Cheap to do; rules it out as a cause.
    - [ ] **K.1.5a.3 — Triage what survives .1 and .2.** Anything still red is genuinely broken — root-cause and fix per-test. Most likely candidates: planted-row generators in `account_recon/demo_data.py` whose `days_ago` constants drifted out of sync with the test fixtures, or a true PR mutual-filter regression that the dataset re-create exposed.
    - [ ] **K.1.5a.4 — Add a unit-level guard test for the filter-control rule.** The K.1 deploy bounced three times on QS rejecting `DefaultFilterControlConfiguration` placement — the rule (multi-sheet filter ⇒ default control + CrossSheet controls; single-sheet filter ⇒ no default control + direct Dropdown) is now correctly enforced in `_multi_select_filter_group`, but only the API's CreateAnalysis call validates it. Add a test in `test_account_recon.py` (and a parallel one in `test_recon.py` if PR has the same helper shape) that walks every FilterGroup and asserts the rule by inspection of `to_aws_json()`. Catches the next regression at unit-test speed instead of at deploy time.
- [x] **K.1.5b — Two infra issues surfaced during K.1.5a triage.**
    - [x] **K.1.5b.1 — Aurora warm-up / retry around visual-render waits.** Today's Exceptions occasionally shows transient SQL exceptions when Aurora has scaled down; the e2e suite then sees `wait_for_visual_titles_present` time out because data never hydrates. Two options to weigh: (a) pre-warm Aurora before `./run_e2e.sh` runs (issue a couple of `SELECT 1` against the base tables and the heaviest views), or (b) wrap `wait_for_visual_titles_present` / `wait_for_visuals_present` in a retry that survives one cold-start window. (a) is cheaper and self-explanatory; (b) is more general and protects ad-hoc reruns.
      - Let's do both a should make b cheap and b will catch drift
      - Done: autouse session fixture `warm_aurora` in `tests/e2e/conftest.py` issues `SELECT 1` + `COUNT(*)` against base tables + the heaviest AR views; retry-once wrapper `_retry_on_playwright_timeout` in `tests/e2e/browser_helpers.py` catches `playwright.sync_api.TimeoutError` for `wait_for_visual_titles_present` / `wait_for_visuals_present`.
    - [x] **K.1.5b.2 — Single-app deploy shouldn't recreate the other app's datasets.** Root cause of the K.1.5a.3 triage: `quicksight-gen deploy account-recon` does delete-then-create on **every** dataset (PR + AR) but only redeploys the AR analysis/dashboard. The recreated PR datasets keep the same ARN, but PR's analysis ends up with stale internal references → visuals report "Cannot access data". Same in reverse for `deploy payment-recon`. Fix in `deploy.py`: scope the dataset delete-then-create to the named app (or, if that turns out to be more invasive than expected, document the workaround as "always `deploy --all` after a single-app deploy" in the CLI help).
      - Done: `_dataset_ids_for_apps()` derives per-app DataSetIds from each loaded analysis's `Definition.DataSetIdentifierDeclarations`; `_delete_datasets` / `_create_datasets` now skip files whose ID isn't in the allowed set. Guard test `tests/test_deploy.py::TestDatasetIdsForApps`.
- [x] **K.1.6 — Update AR Handbook walkthroughs.** Done 2026-04-21. All 14 per-check walkthroughs + 3 rollup walkthroughs rewritten under the unified-table template established in K.1.6.a.1; screenshots (22 total) regenerated under the new naming convention; nav re-pointed at the new Today's Exceptions + Trends layout via the K.1.6.x drill-scoping fix. `mkdocs build --strict` passes clean. One latent UX bug surfaced during the rewrites (drill scoping on the unified table not handling per-account-day right-clicks) and was fixed in-flight as K.1.6.x without stalling the per-walkthrough queue.
  - [x] **K.1.6.x — Drill scoping fix for the unified exceptions table.** Done 2026-04-21. New `pArAccountId` parameter + `account_id`-bound filter group on the Transactions sheet; second `_multi_drill_action` (DATA_POINT_MENU trigger) on the Open Exceptions table writes both `pArAccountId` (from `account_id`) and `pArActivityDate` (from `exception_date`); `menu_link_text_format` tint applied on the `account_id` cell to cue the right-click idiom (existing accent on `transfer_id` retained for the left-click). The two system-level drift rollups (Concentration Master Sweep Drift, GL vs Fed Master Drift) carry neither `account_id` nor `transfer_id` and are intentionally left un-drillable — investigate them on the Trends sheet (subtitle says so). Coverage: new parametrized e2e `tests/e2e/test_ar_todays_exc_drill.py` over the 12 covered check_types — for each, filter the unified table to that check_type, dispatch the matching click idiom, and assert the post-drill Transactions row count is strictly less than the unfiltered baseline (catches "drill didn't fire" and "drill fired but param wasn't written" alike). Two new `browser_helpers` (`right_click_first_row_of_visual`, `click_context_menu_item`) carry the right-click + menu-pick pattern. Unit tests updated for the new param + filter-group ID; 393 unit tests pass.
  - [x] **K.1.6.a — Foundation.** Done 2026-04-21. Template prototype + screenshot capture script + 22 fresh PNGs landed under K.1.6.a.1 / .a.2 / .a.3.
    - [x] **K.1.6.a.1 — "Where to look" template.** Done 2026-04-21. Prototyped in `ach-origination-non-zero.md`. Template structure: (1) **Where to look** paragraph names the *Today's Exceptions* sheet, says to set the **Check Type** control to the literal check name, describes what happens (KPI updates, breakdown bar collapses, unified table narrows), and links a single screenshot of the filtered Open Exceptions table at `screenshots/ar/todays-exceptions-filtered-<slug>.png` (path reserved for K.1.6.a.3 to fill in). (2) **What you'll see in the demo** opens with a small column-mapping table for this check (`account_id` → `gl-1810`, `transfer_id` → blank, `primary_amount` → stored balance, etc.) so readers learn how the wide unified schema specializes for each check. (3) **Drilling in** describes the **pale-green tint cue** on `account_id`, says to right-click and choose **View Transactions for Account-Day** (the action's exact `Name` from `_multi_drill_action`), explains the account-day scope, and explains why `transfer_id` is blank (no single transfer represents an EOD residual). Spatial reference removed ("F.5.4 next to it" no longer applies under the unified-table layout). Three legacy per-check screenshot references (KPI / detail table / aging chart) dropped from the markdown; PNG files preserved on disk per K.1.6.a.3 ("keep until K.1.6.e.2 cutover"). 393 unit tests pass.
    - [x] **K.1.6.a.2 — Update `scripts/generate_walkthrough_screenshots.py`.** Done 2026-04-21. 22 SHOTS entries: 3 shared Today's Exceptions (full sheet / breakdown bar / unified table) + 5 Trends (drift timelines / two-sided rollup table / expected-zero rollup table / aging matrix / per-check daily trend) + 14 per-check filtered unified-table shots. Fixed `OUTPUT_ROOT` to `src/quicksight_gen/docs/walkthroughs/screenshots/` (was `docs/walkthroughs/screenshots/`, which doesn't exist). New `mode="full_sheet"` clips a screenshot from `y=0` to the bottom of the lowest visual on the active sheet (avoids 11000px of trailing whitespace from the tall viewport). New `_set_check_type_filter` helper (Select-all → click-target — sidesteps the multi-toggle race the e2e harness's `set_multi_select_values` hits when the listbox starts with everything selected). Per-check shots are ordered AFTER the Trends shots because the Check Type control is CrossSheet (Today's Exceptions ↔ Trends) — capturing Trends before any filter keeps those shots clean. Filename convention locked: `todays-exceptions-{overview,breakdown,table}.png`, `trends-{drift-timelines,two-sided-rollup,expected-zero-rollup,aging-by-check,per-check-by-day}.png`, `todays-exceptions-filtered-<slug>.png` (slug = per-check walkthrough filename in `src/quicksight_gen/docs/walkthroughs/ar/`).
    - [x] **K.1.6.a.3 — Run script, land new PNGs.** Done 2026-04-21. All 22 shots captured into `src/quicksight_gen/docs/walkthroughs/screenshots/ar/` (3 Today's Exceptions + 5 Trends + 14 per-check filtered). Three iterations of the script needed to get there: (1) KPI visuals don't render `analysis_visual_title_label`, so the two rollup shots had to use `title_index=0` against the table sibling instead of `title_index=1`. (2) The Check Type listbox doesn't expose a "Select all" entry, so `_set_check_type_filter` was rewritten to deselect each currently-selected option individually with `^…$`-anchored regex (prefix collision: "Ledger Drift" ⊂ "Sub-Ledger Drift"). (3) MUI virtualizes the listbox to ~10 options at a time, so "Sweep Target Non-Zero EOD" (alphabetically last) needed an async chunked-scroll helper that pre-mounts off-screen options before clicking; reordered `_set_check_type_filter` to select target FIRST then deselect others, so we never hit a 0-selected state that auto-closes the popover. Post-filter `wait_for_visuals_rendered` now wraps in try/except `PWTimeoutError` → 5s settle (a stale `class*=loading` element kept the wait pegged after the Sweep Target filter even though data had rendered). Old per-check PNGs (`<check>-01-kpi.png` family) preserved on disk for K.1.6.e.2 cutover. Aurora was scaled up by user mid-run to reduce cold-query stalls.
  - [x] **K.1.6.b — Rewrite handbook + training.** Done 2026-04-21.
    - [x] **K.1.6.b.1 — `handbook/ar.md` morning routine.** Done 2026-04-21. Rewrote lines 32-48 as a three-paragraph flow: (1) name the two sheets — Today's Exceptions (row-level operational view) + Exceptions Trends (rollup / time-series view); (2) start on Today's Exceptions, read the big-number KPI, scan the *Exceptions by Check* breakdown bar, browse the *Open Exceptions* unified table with the Check Type sheet control; (3) if non-zero, glance Trends for the rollups (Balance Drift Timelines, Two-Sided Post Mismatch, Expected-Zero EOD) as "different shape of break" framing, then come back to Today's Exceptions and drill. Card grid below unchanged per plan note. The old "scroll down to per-check sections" language is gone since the per-check blocks don't exist in the unified layout.
    - [x] **K.1.6.b.2 — `Training_Story.md`.** Done 2026-04-21. Line 229 "scans Exceptions" → "scans **Today's Exceptions** for the day's totals, glances **Exceptions Trends** for the shape of any breaks, drills into anything aging past their threshold" (bullet under GL Recon persona). Also updated the "Mapping to AR exception checks" intro paragraph (was "All follow the existing AR visual pattern: KPI count + detail table + horizontal aging bar. Drift checks add a timeline.") to describe the new unified layout: every check's rows surface on the Today's Exceptions *Open Exceptions* table with `days_outstanding` + `aging_bucket`, and Trends carries the aging matrix + per-check daily counts + drift timelines. Mapping table itself (line 188-193) was already written in descriptive terms so it reads fine as-is.
  - [x] **K.1.6.c — Rewrite 14 per-check walkthroughs.** Done 2026-04-21. All 14 entries (c.1 through c.14) rewritten under the new template: per-check filtered Open Exceptions screenshot, column-mapping table fronting What-you'll-see, drill description matched to the actual cell hints in the unified table (right-click on pale-green `account_id`, left-click on accent-text `transfer_id`, or "no per-row drill — cross-check this companion check" for the two system-level aggregates c.7 and c.11). Story / What-it-means / Next step / handoff team preserved across all 14. Sub-ledger drift (c.1) was the K.1.6.b pilot; ach-origination-non-zero (c.8) was the K.1.6.a.1 prototype; the remaining 12 were rewritten in one batch under c.2-c.7 + c.9-c.14.
    - [x] **K.1.6.c.1 — `sub-ledger-drift.md`** Done 2026-04-21. Pilot rewrite under the K.1.6.a.1 template. *Where to look* now points at Today's Exceptions + the Check Type sheet control; single filtered-table screenshot replaces the old KPI/detail/aging trio. *What you'll see in the demo* opens with a column-mapping table for this check (`account_id` = subledger_account_id, `account_level` = "Sub-Ledger", `transfer_id` = blank, `primary_amount` = drift, `secondary_amount` = stored_balance). Story / What-it-means / Next-step / handoff team preserved verbatim except for the spatial reference scrub ("Sub-Ledger Drift Days KPI ... next to Ledger Drift Days" gone). *Drilling in* describes the right-click + "View Transactions for Account-Day" path, suggests right-clicking the *oldest* row first to trace the gap to its origin day. Related-walkthroughs entry for Balance Drift Timelines Rollup repointed to the Trends sheet (was previously framed as "lives nearby on the sheet and is often confused").
    - [x] **K.1.6.c.2 — `ledger-drift.md`** Done 2026-04-21. Same template as c.1: filtered table screenshot replaces KPI/detail/aging trio; column-mapping table fronts What-you'll-see (`account_id`=ledger_account_id, `account_level`=Ledger, `transfer_id`=blank, `primary_amount`=drift, `secondary_amount`=stored_balance); Drilling-in describes right-click → View Transactions for Account-Day with the same "right-click oldest first" idiom; Related now repoints Balance Drift Timelines Rollup at the Trends sheet.
    - [x] **K.1.6.c.3 — `non-zero-transfers.md`** Done 2026-04-21. Filtered to `Non-Zero Transfer` (yellow severity); column-mapping notes `account_id`=blank, `account_level`=System, `transfer_id`+`transfer_type` populated, `primary_amount`=net_amount; Drilling-in is left-click on `transfer_id` → Transactions sheet (no right-click — `account_id` is blank for this check). Demo's mix of failed-leg / off-amount / stuck-step error-class explanation preserved. Related now points stuck-step rows at Stuck in Internal Transfer Suspense and sweep-drift rows at Concentration Master Sweep Drift.
    - [x] **K.1.6.c.4 — `sub-ledger-limit-breach.md`** Done 2026-04-21. Filtered to `Sub-Ledger Limit Breach` (amber severity, single-bar collapse); column-mapping highlights amber/breach-only `transfer_type` column + `secondary_amount`=daily_limit; planted-breach table preserved (Bigfoot Brews wire / Pinecrest ach / Big Meadow Dairy cash); Drilling-in is right-click `account_id` → View Transactions for Account-Day, with note that `transfer_id` is blank because breach is a daily aggregate not one transfer.
    - [x] **K.1.6.c.5 — `sub-ledger-overdraft.md`** Done 2026-04-21. Filtered to `Sub-Ledger Overdraft` (red, sticky); column-mapping has `primary_amount`=stored_balance (negative), `secondary_amount`=blank; Drilling-in is right-click `account_id` → View Transactions for Account-Day with the "right-click oldest first" idiom for finding the cover day; planted Harvest Moon / Sasquatch Sips / Cascade Timber Mill ZBA incidents preserved.
    - [x] **K.1.6.c.6 — `sweep-target-non-zero.md`** Done 2026-04-21. Filtered to `Sweep Target Non-Zero EOD` (orange, sticky); column-mapping has `primary_amount`=stored_balance, `secondary_amount`=blank; Drilling-in is right-click `account_id` → View Transactions for Account-Day with the "right-click oldest first" idiom; planted Big Meadow Dairy Operating Main + North incidents preserved.
    - [x] **K.1.6.c.7 — `concentration-master-sweep-drift.md`** Done 2026-04-21. Filtered to `Concentration Master Sweep Drift` (red, system-level aggregate). Critical: both `account_id` AND `transfer_id` are blank for this check, so neither right-click nor left-click drill applies on the unified row directly. Drilling-in walks the reader through the cross-check path: switch Check Type to `Non-Zero Transfer`, find the same dates as `clearing_sweep` rows (`ar-zba-sweep-0004/0017`), then left-click drill to Transactions from there. Also points at the Trends sheet for the timeline view.
    - [x] **K.1.6.c.8 — `ach-origination-non-zero.md`** Done 2026-04-21 (prototyped under K.1.6.a.1). Filtered to `ACH Origination Settlement Non-Zero EOD` (orange); column-mapping has `account_id`=gl-1810 (Ledger), `transfer_id`=blank, `primary_amount`=stored_balance; Drilling-in is right-click `account_id` → View Transactions for Account-Day. No further changes needed in this batch — this entry was the original template prototype for the whole K.1.6.c stack.
    - [x] **K.1.6.c.9 — `ach-sweep-no-fed-confirmation.md`** Done 2026-04-21. Filtered to `ACH Sweep Without Fed Confirmation` (yellow); column-mapping notes `account_id`=blank (System), `transfer_id`=sweep_transfer_id (populated), `primary_amount`=sweep_amount; Drilling-in is left-click `transfer_id` → Transactions sheet for the internal sweep legs, then walk the parent_transfer_id manual filter to confirm no Fed-side child landed.
    - [x] **K.1.6.c.10 — `fed-card-no-internal-catchup.md`** Done 2026-04-21. Updated check title to `Fed Activity Without Internal Catch-Up` to match the unified-dataset literal exactly (the handbook card copy was slightly off — said "Internal Post" rather than "Internal Catch-Up"). Filtered (yellow); column-mapping has `transfer_id`=fed_transfer_id (populated), `primary_amount`=fed_amount; Drilling-in is left-click `transfer_id` → Transactions sheet for the Fed observation legs, then walk parent_transfer_id manually to confirm no internal catch-up child.
    - [x] **K.1.6.c.11 — `gl-vs-fed-master-drift.md`** Done 2026-04-21. Filtered to `GL vs Fed Master Drift` (red, system-level aggregate). Like c.7 (Concentration Master Sweep Drift), both `account_id` and `transfer_id` are blank — Drilling-in walks the reader through cross-checking against `Fed Activity Without Internal Catch-Up` (per-transfer view) for the actual drill. Also points at the Trends sheet for the cumulative timeline view.
    - [x] **K.1.6.c.12 — `stuck-in-internal-transfer-suspense.md`** Done 2026-04-21. Updated check literal to `Internal Transfer Stuck in Suspense` (the unified-dataset literal differs from the handbook card title "Stuck in Internal Transfer Suspense" — used the dataset literal so the reader's filter setting matches the screenshot). Filtered (yellow); column-mapping has `transfer_id`=originate_transfer_id (populated), `primary_amount`=originate_amount; Drilling-in is left-click `transfer_id` → Transactions sheet for the Step 1 originate legs.
    - [x] **K.1.6.c.13 — `internal-transfer-suspense-non-zero.md`** Done 2026-04-21. Filtered to `Internal Transfer Suspense Non-Zero EOD` (orange, sticky); column-mapping has `account_id`=gl-1830 (Ledger), `transfer_id`=blank, `primary_amount`=stored_balance; Drilling-in is right-click `account_id` → View Transactions for Account-Day, with cross-reference to the per-transfer Stuck in Internal Transfer Suspense view for "which specific originates."
    - [x] **K.1.6.c.14 — `internal-reversal-uncredited.md`** Done 2026-04-21. Updated check literal to `Internal Reversal Uncredited` (handbook card title is "Reversed Transfers Without Credit-Back"; used the dataset literal for the filter-setting instruction). Filtered (yellow). Critical structural note: the unified table only carries `originate_transfer_id` as `transfer_id`; it does NOT carry the reversal_transfer_id. Drilling-in clicks the originate (Step 1 legs) then explicitly walks the reader through finding the reversal manually (`ar-on-us-step2-XX` matching the `ar-on-us-orig-XX` suffix). Also notes the same incident surfaces as a Non-Zero Transfer row for the reversal_transfer_id (failed_leg_count > 0).
  - [x] **K.1.6.d — Rewrite 3 rollup walkthroughs (now on Trends sheet).** Done 2026-04-21. All 3 rollups (drift timelines / two-sided post mismatch / expected-zero EOD) re-pointed at the Exceptions Trends sheet — subtitle changed from "Account Reconciliation Exceptions sheet" → "Account Reconciliation Exceptions Trends sheet"; *Where to look* describes Trends as the sister sheet to Today's Exceptions and locates each rollup by position (top / second / third); *Drilling in* changed from "scroll down the Exceptions sheet to the per-check section" (no longer applies — per-check blocks were removed in K.1.4) to "switch to the Today's Exceptions sheet and set Check Type in the Controls strip to <literal>." Each rollup's `source_check` values explicitly mapped to the matching dataset literal an operator would set as a Check Type filter.
    - [x] **K.1.6.d.1 — `balance-drift-timelines-rollup.md`** Done 2026-04-21. Subtitle re-pointed at Trends sheet. *Where to look* names Trends as sister to Today's Exceptions and identifies the rollup as the very first visual at the top of the sheet. *Drilling in* preserved "this is a chart, not a row table — there's no row-click drill" but changed the cross-check path from "scroll down" to "switch to Today's Exceptions and set Check Type to `Concentration Master Sweep Drift` / `GL vs Fed Master Drift`" (the per-check daily aggregates) or `Non-Zero Transfer` / `Fed Activity Without Internal Catch-Up` (the per-transfer underlying view). Mentions the Aging Matrix + Per-Check Daily Trend below this rollup as the supplementary lens. Related-walkthroughs list includes the other two rollups co-located on the Trends sheet.
    - [x] **K.1.6.d.2 — `two-sided-post-mismatch-rollup.md`** Done 2026-04-21. Subtitle re-pointed at Trends sheet. *Where to look* identifies the rollup as the second block from the top — between Drift Timelines (above) and Expected-Zero EOD (below). *Drilling in* changed from "scroll down the Exceptions sheet" to "switch to Today's Exceptions and set Check Type to `ACH Sweep Without Fed Confirmation` (for `source_check = ach_sweep_no_fed_confirmation` rows) or `Fed Activity Without Internal Catch-Up` (for `source_check = fed_card_no_internal_catchup` rows)" + note that the per-check Open Exceptions row carries the transfer ID for left-click drill into Transactions. Related-walkthroughs reframes the other two rollups as co-located neighbors.
    - [x] **K.1.6.d.3 — `expected-zero-eod-rollup.md`** Done 2026-04-21. Subtitle re-pointed at Trends sheet. *Where to look* identifies as the third rollup at the top, after Drift Timelines and Two-Sided Post Mismatch. *Drilling in* enumerates the three `source_check` → Check Type mappings (`sweep_target_nonzero` → `Sweep Target Non-Zero EOD`, `ach_orig_settlement_nonzero` → `ACH Origination Settlement Non-Zero EOD`, `internal_transfer_suspense_nonzero` → `Internal Transfer Suspense Non-Zero EOD`) with a note that all three carry a right-click drill on `account_id` for the per-account-day Transactions view. Related-walkthroughs reframes the other two rollups as co-located neighbors.
  - [x] **K.1.6.e — Nav + cleanup.** Done 2026-04-21.
    - [x] **K.1.6.e.1 — `mkdocs.yml` grouping.** Done 2026-04-21. Kept the AR Handbook entries flat (overview + 3 rollups + 13 per-check = 17 items) per the original lean — operators search by check name, not sheet. Two adjustments: (1) renamed the Fed Activity nav label from "Fed Activity Without Internal Post" to "Fed Activity Without Internal Catch-Up" so the nav label matches the walkthrough H1 + the dataset literal an operator would set as a Check Type filter (no other label drift). (2) Added `navigation.tabs` to `theme.features` — the five handbooks (AR / PR / Data Integration / Customization / Training) now render as horizontal tabs at the top of the page, and the left sidebar only shows the current handbook's entries (17 max for AR vs ~50 across all handbooks before). Material's site-search box at the top still works across the whole docs site regardless of nav structure, so flat-for-search is preserved. Tradeoff: one extra click to switch handbooks.
    - [x] **K.1.6.e.2 — Delete orphan PNGs.** Done 2026-04-21. Dropped 40 old per-check PNGs (`<check>-01-kpi.png`, `<check>-02-table.png` or `<check>-02-timeline.png`, `<check>-03-aging.png` for each of 14 checks; `gl-vs-fed-master-drift` and `concentration-master-sweep-drift` only had `-01-kpi` + `-02-timeline`). Kept: 3 `daily-statement-*.png` (referenced by the ETL `how-do-i-validate-a-single-account-day` walkthrough), 17 `todays-exceptions-*.png` (3 shared overview shots + 14 per-check filtered shots referenced by the new walkthroughs), 5 `trends-*.png` (drift timelines / two-sided rollup / expected-zero rollup / aging matrix / per-check daily trend — captured for handbook + Trends-sheet rollup walkthrough use). Net 25 PNGs remain in `src/quicksight_gen/docs/walkthroughs/screenshots/ar/`.
    - [x] **K.1.6.e.3 — Local mkdocs build + visual-check.** Done 2026-04-21. `.venv/bin/mkdocs build --strict` passes clean (build time 0.53s). Strict mode treats any warning as an error, so the clean build confirms every rewritten walkthrough renders, every cross-walkthrough link resolves, and every screenshot reference in the 14 per-check + 3 rollup walkthroughs points at an existing PNG (no orphan refs after the K.1.6.e.2 delete). Material team's standard MkDocs 2.0 deprecation banner is unrelated to the project's build health.
- [x] **K.1.7 — Relicense to MIT, release the next version.** Done 2026-04-21 (file changes). License: `LICENSE` replaced with standard MIT text (Copyright © 2026 Christopher Hotchkiss); `pyproject.toml` `license = "Unlicense"` → `"MIT"`. Classifier: `Development Status :: 4 - Beta` → `5 - Production/Stable`. Version: `__version__` 3.4.0 → 3.5.0 (minor — AR Exceptions split + handbook rewrite + license change, no breaking API). RELEASE_NOTES.md: v3.5.0 entry added at top rolling up Phase K.1 (K.1.0–K.1.7); also backfilled v3.4.0 (docs + training kit in wheel + export commands) and v3.3.0 (Customization Handbook) entries — the gap from v3.2.2 to now wasn't being captured. Audit: only 2 runtime deps (`click` BSD-3-Clause, `pyyaml` MIT) + all optional deps MIT-compatible; no source files carry license headers needing update. Wheel build clean — `dist-info/licenses/LICENSE` ships; METADATA shows `License-Expression: MIT` and `Development Status :: 5 - Production/Stable`. 393 unit tests pass. Tag push (`git tag v3.5.0 && git push origin v3.5.0`) triggers the release pipeline (build → TestPyPI → manual PyPI approval → GitHub Release) — held for explicit go-ahead.

## K.2 — Cross-sheet navigation parameter hygiene

Goal: make every cross-sheet drill deterministically scope the destination sheet by writing the **full** parameter set the destination filters on (including explicit defaults for params the source doesn't care about), instead of only writing the params the source visual chose. Today, an unwritten param silently retains its prior value and quietly filters the destination — a "stale param stacking" failure mode that's hard to spot in screenshots and surfaces hours after the click as a row count that doesn't match what the user expected.

Eight AR sheet-state params today: `pArAccountId`, `pArActivityDate`, `pArLedgerAccountId`, `pArSubledgerAccountId`, `pArTransferId`, `pArTransferType`, `pArDsAccountId`, `pArDsBalanceDate`. PR has its own set (`pPr*`). Each drill action currently writes only what it cares about; a follow-on drill from a sheet that doesn't write `pArTransferType` will inherit whatever it was last set to.

User flagged this as needing an early spike — there's real uncertainty in whether QuickSight's `SetParameterValueConfiguration` supports an explicit "reset to default" semantic, or whether the action has to manufacture a literal default value (empty string, `null`, sentinel). The spike answers that before any migration work begins.

- [ ] **K.2.0 — Spike: validate the param-reset approach.** Pick one drill (suggest the Today's Exceptions right-click → Transactions, since it already touches 2 of 8 AR params). Set up a stale-param scenario: visit the Transactions sheet via a path that pre-sets `pArTransferType`, then drill from Today's Exceptions which doesn't write that param today. Modify the drill to set **all 8** AR params (the 2 it cares about + 6 explicit defaults). Browser-verify in Playwright that the destination renders unfiltered on the 6 defaulted params. **Critical question to answer:** does QS support setting a param to "default" via `SetParameterValueConfiguration` (empty / null / "All"), or does the action have to write a literal sentinel? If the latter, K.2.1 needs a per-param default registry. **Exit criterion:** one working drill that demonstrably defeats stale-param leak in browser e2e + a one-page note in the spike commit message recording which approach won and why.
- [ ] **K.2.1 — Inventory destination-sheet param sets.** Enumerate every `CustomActionNavigationOperation` + `CustomActionSetParametersOperation` across `payment_recon/` and `account_recon/` analysis modules. For each *destination* sheet_id, list the param set the sheet's filter groups are bound to. Land as `common/sheet_params.py` (new module): `SHEET_PARAMS: dict[str, list[str]]` keyed by sheet_id. If K.2.0 picked the literal-default approach, also land `PARAM_DEFAULTS: dict[str, str | None]` here.
- [ ] **K.2.2 — Build `cross_sheet_drill()` helper in `common/`.** Signature: `cross_sheet_drill(action_name: str, target_sheet_id: str, set_params: dict[str, ColumnRef | str], trigger=DATA_POINT_CLICK) -> VisualCustomAction`. Resolves `SHEET_PARAMS[target_sheet_id]`, emits one `SetParameterValueConfiguration` per param (caller's value where supplied; default per K.2.0/K.2.1 outcome elsewhere). Wraps in the existing `CustomActionNavigationOperation` shape so callers swap in mechanically.
- [ ] **K.2.3 — Migrate all drill sites to `cross_sheet_drill()`.** ~20 sites estimated (every DATA_POINT_CLICK + DATA_POINT_MENU navigation across PR + AR). Mechanical refactor — call the helper instead of building `SetParametersOperation` lists by hand. Existing unit tests in `test_account_recon.py` / `test_recon.py` should still pass; if any break it's because they were asserting the old narrow-write shape — update the assertions.
- [ ] **K.2.4 — Browser e2e for stale-param hygiene.** New `tests/e2e/test_cross_sheet_param_hygiene.py` (one representative test per source→destination sheet pair, not one per drill — combinatorial otherwise). Pattern: pre-condition the destination sheet with a known stale param via direct visit, drill from the source, assert the destination row count matches the unfiltered baseline (for the params the drill defaults). Tests gated under `QS_GEN_E2E=1` like the rest of the e2e suite.
- [ ] **K.2.5 — Release as v3.5.3 (patch).** No schema change, no user-facing dashboard change, no handbook change — drill behavior just becomes more reliable. RELEASE_NOTES.md entry frames it as a UX correctness fix.

## K.3 — Lateness as a data column, not an operator threshold

Goal: bake "is this row overdue" into the data instead of leaving it to operator scanning of `days_outstanding`. Add an optional `expected_complete_at TIMESTAMP` to the `transactions` schema; default to `posted_at + INTERVAL '1 day'` when null; for multi-leg transfers, the earliest debit leg's `expected_complete_at` wins as the transfer-level tie-breaker. Datasets gain an `is_late BOOLEAN` predicate so exception checks switch from operator-applied day thresholds to a clean data-driven flag.

Today: PR has `late_default_days` (default 30) as a config knob + slider; AR doesn't have an explicit "late" concept at all — operators eyeball `days_outstanding` and `aging_bucket` and apply their own threshold. Both apps would benefit from data that says when a row tipped overdue, with a deterministic tie-breaker for multi-leg events. The column is optional so ETL teams can adopt incrementally; existing data stays valid.

- [ ] **K.3.0 — Schema design + portability check.** Add `expected_complete_at TIMESTAMP NULL` to `transactions` in `schema.sql`. **Not** added to `daily_balances` — those are point-in-time snapshots, lateness is per-leg. Default formula: `COALESCE(expected_complete_at, posted_at + INTERVAL '1 day')`. Sanity-check the `INTERVAL` literal portability against the project's portability constraint (no JSONB / no Postgres-specific syntax) — `INTERVAL '1 day'` is standard SQL but worth confirming in the more conservative target RDBMS family. Document the column in `docs/Schema_v3.md` as optional with the multi-leg tie-breaker rule (earliest debit leg's `expected_complete_at` becomes the transfer-level expected completion).
- [ ] **K.3.1 — Both demo generators populate `expected_complete_at`.** `payment_recon/demo_data.py` + `account_recon/demo_data.py`: realistic mix of null (most rows — uses default), explicit same-day for instant rails (Fed wires within an hour), explicit T+2 for ACH, explicit T+3 for cards. Re-lock the per-app SHA256 hashes in `test_demo_data.py::TestDeterminism`.
- [ ] **K.3.2 — Datasets surface `is_late` + `expected_complete_at`.** Add `is_late BOOLEAN` and `expected_complete_at TIMESTAMP` columns to `ar_unified_exceptions` and the relevant per-check views; same in PR's exception datasets. The `is_late` predicate is `CURRENT_TIMESTAMP > COALESCE(expected_complete_at, posted_at + INTERVAL '1 day')`. Update `DatasetContract` entries. SQL projections that today use `(CURRENT_DATE - posted_at::date) > N` switch to `is_late = true`.
- [ ] **K.3.3 — KPIs / filters / visuals consume `is_late`.** PR Payment Reconciliation tab's "Late Payments" KPI uses `is_late` instead of `late_default_days` slider math. Today's Exceptions sheet adds an `is_late` filter control (toggle: All / Late only / On-time only). Decide what happens to `late_default_days` config: probably retire (the data answers; the slider only ever existed because the data didn't). If kept for back-compat it lives on as a fallback in views that don't surface `is_late`.
- [ ] **K.3.4 — Handbook updates.** PR + AR walkthroughs update language: "rows where `days_outstanding > N`" → "rows where `is_late = true`". `handbook/customization.md` adds a section on the `expected_complete_at` ETL contract. `Schema_v3.md` already updated in K.3.0 with the column spec; cross-link from the AR/PR walkthrough overviews.
- [ ] **K.3.5 — Release as v3.6.0 (minor).** Additive schema change — the column is NULLABLE with a default formula, so all existing SQL keeps working with the COALESCE semantics. Bump is minor not patch because the dataset contract gains a column (consumers parsing the contract may notice). RELEASE_NOTES.md describes the new column, the data-driven `is_late` predicate, and the `late_default_days` deprecation (or retention if K.3.3 keeps it).

## K.4 — Investigative app (AML + general)
(Note before we start on this, that this is broader than just AML — call it the investigative app)

Goal: new `quicksight_gen.aml_recon` app (third app alongside PR + AR), mirroring the existing two-app pattern. Reads from the same `transactions` + `daily_balances` base tables — no schema changes (assuming K.3 has shipped, the new lateness column is already there for free). Build feasibility-driven, not equally-weighted.

- [ ] **K.4.0 — App skeleton.** New `src/quicksight_gen/aml_recon/` package mirroring `account_recon/` layout (`analysis.py`, `visuals.py`, `filters.py`, `datasets.py`, `demo_data.py`, `constants.py`, `etl_examples.py`). Wire into CLI (`generate`, `deploy`, `demo apply` accept `aml-recon` as a third app). New theme preset `sasquatch-bank-aml`. Empty Getting Started sheet + 3 stub sheets.
- [ ] **K.4.1 — Person-to-person fanout sheet.** Easiest: straight aggregation. Dataset: COUNT DISTINCT senders + SUM(amount) per recipient sub-ledger over a chosen window. Visual: KPI (total recipients with N+ distinct senders) + ranked table sorted by fanout count. Filter: window length, fanout threshold.
- [ ] **K.4.2 — Sliding-window statistical anomaly sheet.** Moderate: WINDOW functions + std-dev. Dataset: rolling SUM(amount) per sender→recipient pair over a 2-day window, with a population baseline mean + std-dev computed across the same window family; flag rows where window sum > mean + 2σ. Visual: distribution plot + flagged-rows table. Filter: σ threshold (default 2), window length (default 2 days).
- [ ] **K.4.3 — Money-trail provenance sheet.** Hardest: graph traversal. Dataset shape: recursive CTE walking up/down `parent_transfer_id` chains from a chosen starting transfer_id, **flattened to one row per edge** — `(source_account_id, target_account_id, hop_amount, depth)` — which is the input shape QuickSight's native Sankey visual consumes (Source / Destination / Weight). Visual: Sankey is the headline (one node per touched account, one link per edge, weight = hop_amount); a hop-by-hop detail table sits beside it for the rows Sankey hides plus account/transfer_id legibility. Filter: starting transfer_id, max hops, min hop amount. Fallback if a chosen chain exceeds QuickSight's Sankey node/link cap: surface the hop-by-hop table as the primary view for that query and a "chain too wide for Sankey" notice in the visual subtitle.
- [ ] **K.4.4 — AML demo data extension.** Plant scenario rows exercising each sheet: a fanout cluster (10+ senders → 1 recipient), a 2σ-anomalous window pair, and a 4-hop transfer chain. Add `TestScenarioCoverage` assertions in `test_aml_demo_data.py`. Re-lock the per-app SHA256 hash.
- [ ] **K.4.5 — Browser e2e for AML.** `tests/e2e/test_aml_*.py` mirroring AR's coverage shape. Sheet visuals render, filters scope correctly, drill-downs land.
- [ ] **K.4.6 — AML Handbook section.** New `docs/handbook/aml.md` + `docs/walkthroughs/aml/*.md`. Three walkthroughs minimum, one per sheet's core question. Add to `mkdocs.yml` nav after PR.

**Sequencing.** K.1 shipped (v3.5.0 / v3.5.1 / v3.5.2 — AR Exceptions density refactor + handbook rewrite + MIT relicense + CI hardening). K.2 next: cross-sheet param hygiene is a UX correctness fix that benefits every drill in PR and AR; doing it before K.4 means the new investigative app inherits the helper from day one. K.3 after K.2: lateness is a schema-additive change with handbook + dataset ripple; cleaner to land before greenfield app work writes more SQL against the old "operator threshold" pattern. K.4 last: greenfield app, no in-flight users to disrupt, picks up both the K.2 helper and the K.3 lateness column for free. Within K.4, sub-steps remain ordered fanout → sliding window → provenance, easiest first; ship each sheet incrementally so the app becomes usable after K.4.1, not only at K.4.3.

# Backlog - Phase L Candidates

## Whitelabeling Enhancements
- enhance all the demo data/handbook to be reskinable like the training material can be. don't want to break the crypto hash on the training output, so maybe a string replace after that but before its applied?

## Audit Enhancements
- How can someone show the state of the system durably?
  - This is to support reporting to auditors/regulators
  - should not use the pixel perfect report feature (costs too much money)
  - may just be we add to the training material to pdf print certain tabs to start

## New Persona
See Training_Story.md, the executives want data!
- another persona, metrics across the data
  - Added to the training story personas
  - how many accounts does each ledger have?
  - how many transactions over time?
  - how much money has been moved? (per transaction type too)

## Data Evaluation / Test Enhancements
- Could given a postgresql database connection evaluate a dataset to see if it already has all the exception cases in it? report out on the command line some stats?
- We should include a database warm command with the testing commands
