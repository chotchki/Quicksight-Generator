# QuickSight Generator — Active Plan

**Where we are.** v8.0.0 shipped (Q.3.a CLI redesign — four artifact groups, emit/--execute pattern). Historical detail for every phase prior to v8.0.0 lives in `PLAN_ARCHIVE.md` and `RELEASE_NOTES.md`. This file tracks **forward-looking** work only.

---

## Phase history (one-line per shipped phase)

- **Phase N** (v6.1.0) — Investigation + Executives ported onto L1/L2 tree primitives; theme moved to L2 YAML attribute; preset registry dropped. Full detail: `PLAN_ARCHIVE.md`.
- **Phase O** (v6.2.0) — Unified docs render pipeline with mkdocs-macros + `HandbookVocabulary`; per-app handbooks render against any L2 instance.
- **Phase P** (v7.x cumulative) — Dialect-aware schema + dataset emission; Postgres + Oracle CI matrix; Phase R seed pipeline foundations.
- **Phase Q.1** — Dashboard polish: USD currency formatting via `Measure(currency=True)`, universal date-filter sweep, Oracle case-fold wrapper for `ORA-00904`.
- **Phase Q.2** — Doc IA cleanup; Shape C audience-first home; persona-leak sweep across handbook + walkthroughs.
- **Phase Q.4** (v7.3.0) — Persona-neutral docs release; new `persona:` block on L2 YAML; CI gate for persona-token leakage.
- **Phase Q.5** — Persona-neutral docs full L2-driven substitution; Investigation walkthroughs split into mechanics + worked-example admonitions.
- **Phase Q.3.a** (v8.0.0) — CLI redesign: four artifact groups (`schema | data | json | docs`); each `apply`/`clean` defaults to emit, `--execute` opts in to side effects; `cli_legacy.py` deleted; bundled JSON emit (no per-app filter).
- **Phase R** (v7.2.0) — 90-day per-Rail healthy baseline + embedded plant overlays (densify×5, broken×15, inv-fanout×5); Volume Anomalies signal real on the seed; lognormal amount distribution.
- **Phase S** — Research: drop the system `dot` binary. Spiked Mermaid+ELK (failed eyeball — self-loops floated, layout fidelity poor) and graphviz WASM via `@hpcc-js/wasm-graphviz` (passed — byte-identical to graphviz/dot). Verdict written into RELEASE_NOTES + git log; Phase T executed the migration.
- **Phase T** (v8.1.0) — Every diagram now renders client-side via `@hpcc-js/wasm-graphviz`. `render_*` helpers return DOT strings; `<script type="text/x-graphviz">` blocks inside `<figure>` wrappers; ~50-line JS shim does the WASM render. 5 `apt-get install graphviz` lines deleted across CI / Release / Pages workflows.

_Phase S + T sub-task detail removed during the post-T cleanup. RELEASE_NOTES v8.1.0 carries the migration narrative; the spike outcome is documented in the v8.1.0 release notes (Mermaid+ELK failed eyeball, graphviz WASM passed)._

## Phase U — `quicksight-gen audit` PDF reconciliation report

**Goal.** Add a fifth artifact group `audit` to the CLI. `quicksight-gen audit apply -c config.yaml --execute -o report.pdf` queries the per-prefix matviews + base tables directly, formats the result via `reportlab`, emits a regulator-ready PDF. Bypasses QuickSight pixel-perfect entirely (cost + wrong shape for the auditor).

**Why now.** Phase R landed the realistic baseline; Phase Q polished the dashboard surface; Phase T just proved we can stay system-binary-free. The L1 invariant matviews + typed dataset contracts + persona-neutral templating give us all the inputs needed; the same SQL the dashboards run, executed once at report-generation time, formatted into a printable artifact.

**Why this matters more than a QS pixel-perfect export.** Auditors need: cover summary, per-invariant violation tables with row counts + dollar magnitudes, per-account-day reconciliation walk for every drifted account, supersession audit trail, sign-off block. Different shape than the dashboards; building it as its own artifact (sourced from the same matviews) means the regulator's view + the operator's view stay in sync without QS in the loop.

### Decisions (2026-05-02 planning gate)

| Question | Decision |
|---|---|
| Report shape | Cover + executive summary + per-invariant tables + per-account-day Daily Statement walk + sign-off block. **Build page by page** with a review gate between each. |
| Period | Default `yesterday + last 7 days`. `--from / --to` CLI flags override. |
| PDF tech | **`reportlab`** — pure Python wheels, no system deps (preserves the Phase T win). Programmatic API; layout iterates. |
| CLI shape | `audit apply \| clean \| test`. `apply` defaults to emit Markdown source for inspection; `--execute` writes the PDF. Same `--l2 PATH` flag as the other groups. |
| Provenance | Hash of matview-snapshot SHA256s + generation timestamp + L2 fingerprint embedded in cover page footer. Documented on the site. Optional `audit verify report.pdf -c config.yaml` sub-command for recomputation. |
| Persona | Today's L2 surface (institution name + accent + logo via persona block) is enough — functional > marketing. |
| Test layer | Test the **SQL strings + template-input dicts** (dataset-contract pattern). End-to-end smoke: render PDF, extract text via `pypdf`, assert expected section headings + sentinel values from a known seed. |
| Sign-off | Visual signature line in the PDF. Cryptographic signature applied **after** human review (Adobe Sign / DocuSign / `pyHanko`) — out of scope for the generator. |

**Acceptance.** A v1 PDF an auditor would actually accept: covers the L1 invariants, generated reproducibly, carries a verifiable provenance stamp. Iterate page-by-page within Phase U; add sub-tasks as feedback comes in.

### Execution plan (review gate between each)

- [x] **U.0 — Skeleton + harness.** Add `cli/audit.py` with the `audit apply | clean | test` shell. `audit apply` (no `--execute`) emits a one-line "v1 stub" Markdown source to stdout. `audit apply --execute -o report.pdf` writes a one-page reportlab PDF that says "Phase U skeleton — institution: {name}, period: {from}–{to}". Wire into `cli/__init__.py` alongside the other four groups. Add `reportlab` to `[audit]` extra in `pyproject.toml`. **Review gate:** confirm CLI shape feels right before adding pages.
- [x] **U.1 — Cover page.** Institution name (from L2 YAML persona block), period (default yesterday + last 7 days; `--from / --to` overrides), generation timestamp, L2 instance fingerprint hash placeholder (real hash lands in U.7). Layout: title, subtitle, period band, footer with provenance placeholder. **Review gate.**
- [x] **U.2 — Executive summary page.** Totals across the period: transaction count, dollar volume (gross / net), exception counts by check (drift / overdraft / limit_breach / stuck_pending / stuck_unbundled / supersession). Single page, table layout. SQL queries reuse the same matview shapes the dashboards do. **Review gate.**
- [ ] **U.3 — Per-invariant violation tables.** One page (or grouped) per L1 invariant, sourced from `<prefix>_*` matviews. Columns: account, account role, day, magnitude (dollar amount or count), reason. **Review gate after each invariant lands** so layout iterates per-table.
  - [x] U.3.a — Drift
  - [x] U.3.b — Overdraft
  - [x] U.3.c — Limit breach
  - [x] U.3.d — Stuck pending
  - [x] U.3.e — Stuck unbundled
  - [x] U.3.f — Supersession audit
  - [x] U.3.g — **Vocab + theme templating sweep across all audit PDF sections.** Theme: all hardcoded hexes (`#1a1a1a`, `#eef3f7`, `#c7d6e3`, `#f5f8fb`, `colors.whitesmoke`, `setFillGray(0.4)`) replaced with `theme.<token>` references — `theme = resolve_l2_theme(instance) or DEFAULT_PRESET` resolved once in `audit_apply` and threaded through every `_*_story` function via a new `theme` param. Footer color now respects theme via `_make_footer_drawer(theme)` closure factory (reportlab's `onFirstPage`/`onLaterPages` callbacks couldn't take extra args). Vocab: institution name already came from persona block (no further substitution needed — audit prose is L1-domain generic, not persona-specific). Persona-leak CI: `tests/audit/test_persona_clean.py` runs PDF + markdown emit against `spec_example.yaml`, extracts via pypdf, asserts zero matches in the same blocklist `tests/data/test_seed_persona_clean.py` uses (sasquatch / bigfoot / yeti / snb / frb / etc.). Both PDF and markdown paths covered.
  - [x] U.3.h — **PDF bookmarks + TableOfContents flowable.** `_AuditDocTemplate` proxy creates a `BaseDocTemplate` with an `afterFlowable` hook; tagged headings (via `_bookmarked_h1` / `_bookmarked_h3` helpers, which set `_bookmark_level` on the Paragraph) emit both `canvas.addOutlineEntry()` and `notify('TOCEntry', ...)`. `PageTemplate` carries the existing themed footer. `TableOfContents` flowable inserted on its own page after the cover; `multiBuild()` does the two-pass render (pass 1 collects page numbers, pass 2 fills the TOC). Hierarchy: 7 section H1s at level 0, 13 sub-section H3s at level 1. Cover page Title + the "Table of contents" heading itself stay un-bookmarked. Verified: 10 outline entries + 13 sub-entries + dot-leader TOC page on the sasquatch_pr live PDF.
- [x] **U.4 — Per-account-day Daily Statement walk.** For each (account, business_day) row in `<prefix>_drift` for the period, audit emits a walk page: 5 KPIs (Opening / Debits / Credits / Closing stored / Drift) sourced from `<prefix>_daily_statement_summary` matview + day's transactions from `<prefix>_current_transactions`. Same matview reads as the L1 dashboard's Daily Statement sheet → row-for-row agreement. Walks bookmarked at outline level 1 under "Per-account Daily Statement walk" level 0 (each `account_id — YYYY-MM-DD` is its own bookmark + TOC entry). **Footnote explains the per-day drift here is `closing_stored − closing_recomputed-from-day's-flow` which can differ from U.3.a's cumulative `stored − sum(all transactions)` when daily_balances are sparse.** Sasquatch_pr live: 2 walks (cust-0001 / 04-27, cust-0002 / 04-26) — page count went 10 → 14.
  - [x] **Parents always render, even with zero drift — internal-scope only.** Walks are now the union of (drift matview pairs) + (every internal-singleton parent × every business_day in the period). Internal parents (L2 ``Account`` singletons with ``scope: "internal"`` — GL clearing, concentration, ZBA master) get a walk page even when drift is zero, because their day-by-day balance walk is itself auditor-relevant; a clean walk is evidence of correctness. **External counterparty singletons (``scope: "external"``) are out of scope for reconciliation and do NOT get walks** — they're not in the operator's books. New `_internal_singleton_account_ids(instance)` helper threaded into `_query_daily_statement_walks`. Section intro prose (markdown + PDF) calls out both rules. Sasquatch_pr live: 14 → 52 pages (6 internal GL parents × 5-7 days + 2 customer-drift walks); without the scope filter would have ballooned to 176.
- [x] **U.5 — Sign-off block.** Final-page sign-off section, **two stacked blocks**: (a) **System attestation** — themed-panel table with Institution / Reporting period / Generated by (`quicksight-gen v8.1.0`) / Generated at (ISO timestamp) / L2 instance (path basename, or `<instance> (bundled)` when no path passed) / Provenance fingerprint placeholder (Courier, escaped angle brackets so reportlab's mini-XML doesn't eat the `<pending — see Phase U.7>` literal). Footnote calls out "external cryptographic seal — see Phase U.7" so the reader knows what U.7 will cover and what stands now. (b) **Auditor attestation** — printable form with 5 labelled rows (Auditor name / Title / Organization / Date reviewed / Signature) each carrying a `LINEBELOW` rule on the value column for a real signature line, plus a tall bordered Notes / exceptions box. Footnote: "May be left blank if the report was generated unattended (e.g. an automated pipeline) — the system block above stands on its own." Splitting the two means an automated pipeline can ship a signed system block without forging an auditor signature; the auditor can sign the human block without invalidating the machine attestation. Sign-off bookmark + TOC entry at level 0 (alongside Drift / Overdraft / etc.). Sasquatch_pr live: 14 → 15 pages. Markdown emit mirrors with two ``###`` sub-headings, an underline-padded table for the auditor fields, and a fenced underline block for notes. **Test: 12 → 12 (additive assertions)** in `tests/audit/test_cli_smoke.py` covering both markdown + PDF (system + auditor headings, version stamp, blank form fields, notes box).
- [x] **U.6 — Provenance footer + cover-page provenance block.** Per-page footer rebuilt via the **NumberedCanvas** pattern (subclass `reportlab.pdfgen.canvas.Canvas`, defer `showPage` so the buffered page state piles up in `_saved_pages`, then in `save` iterate the buffer with the now-known total). Standard reportlab `onPage` callbacks can't do "Page N of M" because the per-page hook only sees the current page during build; even `multiBuild` doesn't expose the final total. Footer left: `quicksight-gen vX.Y.Z  ·  Generated YYYY-MM-DD HH:MM`. Footer right: `Page N of M  ·  Provenance: pending` (short form). New `_short_fingerprint_placeholder()` returns the compact `pending` stand-in distinct from the long-form `<pending — see Phase U.7>` so a sweep that resolves one when U.7 lands doesn't accidentally rewrite the other. Cover page also gets a new `Source-data provenance` themed-panel table listing the four sources that fully determine this report's contents (Transactions table, Daily balances table, L2 instance YAML, quicksight-gen code), each with `Last entry / version` + `SHA256` columns showing the long-form `<pending>` placeholder for U.7 to fill in. Sasquatch_pr live: page count unchanged at 52 (provenance block fits on cover); footer chrome now stamps every page including TOC + sign-off.
- [x] **U.7 — Provenance hash + verify subcommand.**
  - **Fingerprint scope (locked):** hashes the **base tables + external inputs**, NOT matviews. The four inputs: (1) `<prefix>_transactions` rows `WHERE entry <= MAX(entry)` at audit time, (2) `<prefix>_daily_balances` rows `WHERE entry <= MAX(entry)`, (3) L2 yaml file bytes verbatim, (4) `quicksight-gen` code identity (`v{__version__}+g{git_short}` when running from a git checkout, falls back to `v{__version__}` for pip installs).
  - **Per-table column discovery is runtime, alphabetical-by-lowercased-name** via `cur.description` (DB-API 2.0 standard, portable PG + Oracle). Hardcoded column lists were a footgun: a new column added to a base table would silently be excluded from the hash, producing a fingerprint that claimed "binds to all source data" while missing whatever the new column carried. Lowercasing before sorting handles PG (returns lowercase) vs Oracle (returns UPPERCASE).
  - **Per-cell canonicalization** (`_canonical_value`): `Decimal` via `str()`, `date`/`datetime` via `isoformat()`, `bool` → `"1"`/`"0"`, `None` → empty bytes. Field separator `\x1f`, row separator `\x1e` — control codes that can't appear in our schema's data so escaping isn't needed. Row stream uses `for row in cur: h.update(...)` so memory stays flat regardless of row count.
  - **Composite fingerprint:** `ProvenanceFingerprint.composite_sha = SHA256(tx_hwm || tx_sha || bal_hwm || bal_sha || l2_yaml_sha || code_identity)` (each on its own labeled line). Short form (footer) = first 8 hex chars via `.short`; long form (cover provenance + sign-off + verify) = full 64-hex via `.composite_sha`.
  - **Embedded in PDF metadata `/Subject`** as JSON via `to_dict()` so `audit verify` extracts it without needing the source code that generated it. `from_dict()` rejects unknown `schema:` versions to fail loud rather than silently misverify a future-format PDF.
  - **`audit verify report.pdf -c config.yaml [--l2 path]`** — extracts embedded provenance from `/Subject`, sanity-checks that the embedded high-water marks haven't been truncated below in current DB (rows the report bound to are gone → error), recomputes each input against current sources at the EMBEDDED hwm (so verification reproduces the report's snapshot point), compares. Exit 0 on full match, 1 with per-source diff on mismatch. Each diff shows label + embedded vs current. Live verified on sasquatch_pr: happy path matches; tampered L2 yaml triggers `l2_yaml_sha` diff with exit 1; building at a later commit triggers `code_identity` diff.
  - **Matview-evidence sidecar deferred** — base-table fingerprint is the authoritative binding; the matview "is this matview consistent with base tables" check belongs in `data refresh --check` or similar (separate concern from report provenance). Tracked as a backlog item under M.2d if needed.
  - **Test coverage:** 17 tests pass (13 → 17, +4 for verify). New: `test_audit_verify_errors_on_pdf_without_provenance`, `test_audit_verify_errors_on_missing_pdf`, `test_provenance_fingerprint_round_trips_through_dict`, `test_provenance_fingerprint_rejects_unknown_schema`. Live e2e (verify-against-tampered-row-in-DB) deferred to U.8 since it needs a writable DB fixture.
  - **Docs (deferred to U.10):** `docs/handbook/audit.md` will cover what the fingerprint covers, how to recompute manually, exact SQL `audit verify` runs.
- [x] **U.7.5 — Extract provenance + pdf-chrome modules + split cli/audit.py into a package.** Two-stage: **(a)** lifted `common/provenance.py` (291 lines, full fingerprint primitives) + `common/pdf/audit_chrome.py` (156 lines, `BookmarkedDocTemplate` + `bookmarked_h1`/`h3` + `make_footer_drawer`) out of cli/audit.py. Drops the leading underscore on moved names. Title-Case sweep on every section heading + sign-off sub-headings as bonus polish. **(b)** Converted cli/audit.py (4077 lines, single file) into the cli/audit/ package: `__init__.py` (1573 — CLI cmds + dataclasses + queries), `markdown.py` (751 — `_render_*_markdown`), `pdf.py` (1845 — `_*_story` + `_write_audit_pdf` + `_cover_logo_flowable` + `_provenance_block_story` + `_signoff_story`). Cyclic imports avoided via deferred `from .pdf import _write_audit_pdf` inside `audit_apply` body; `markdown.py`/`pdf.py` import dataclasses from `__init__.py` at module top (works because init defines them before deferred imports run). Single biggest file dropped from 4466 → 1845 lines. All 17 audit tests pass; live PDF identical (composite_sha == ac5a15f7…).
- [x] **U.7.b — Auto-sign PDF (key sourced from config.yaml).** When `config.yaml` carries a `signing:` block (PEM RSA `key_path` + PEM `cert_path`, optional `passphrase_env` + `signer_name`), `audit apply --execute` runs the rendered PDF through pyHanko's incremental signer. Field name `QSGSystemSignature`. When `signing:` is absent the PDF ships unsigned (back-compat — every existing config still works). **Multi-signature workflow:** subsequent reviewers stack additional signatures on top via Adobe Acrobat / pyHanko / any compliant tool — the document is deliberately silent on how many signatures are required (no "1 of N" hints). **Sign-off block redesign:** dropped the labelled Auditor name / Title / Organization / Date reviewed / Signature rows since the PDF signature dictionaries already bind signer name + signing time; "Reviewer Attestation" prose now points the reviewer at their PDF reader's signing tool. Notes / Exceptions box stays as the human surface for review comments. Self-signed test fixture committed under `tests/audit/fixtures/test-signing-{key,cert}.pem` (CN=quicksight-gen audit test signing); both `run/config.{postgres,oracle}.yaml` reference it. **Tests: 17 → 20** (`test_audit_apply_signing_block_loads_into_config`, `test_audit_apply_signing_block_missing_field_errors`, `test_audit_apply_execute_signs_pdf`). Live verified on sasquatch_pr: signed PDF carries 1 embedded signature + still passes `audit verify` (signing leaves `/Subject` metadata intact). New module: `common/pdf/signing.py` (~75 lines). New dep: `pyhanko` (added to `[audit]` extra). **Notes-as-fillable-form-field deferred** as a future polish — current notes box is a styled cell.
- [x] **U.7.c — Provenance appendix page + L2 yaml attachment + fillable notes form.** New back-of-PDF appendix page (after Sign-off, bookmarked + TOC entry at level 0). Sasquatch_pr live: pages 52 → 54 (Sign-Off → page 52, Provenance Appendix → page 53). Three sections + an attachment + an AcroForm field:
  - **(a) Matview Evidence sidecar** — `MatviewEvidence(matview, row_count, sha256)` for each of `current_transactions`, `current_daily_balances`, `drift`, `ledger_drift`, `overdraft`, `limit_breach`, `stuck_pending`, `stuck_unbundled`, `daily_statement_summary`. New `hash_matview_rows(cur, matview)` helper in `common/provenance.py` — pulls all rows, sorts canonically (alphabetical-by-lowercased column name + tuple-lex on canonicalized cells), streams into SHA256. Distinct from the authoritative composite fingerprint; a divergence is a *technical* signal (stale matview / schema drift) rather than a binding problem.
  - **(b) Reproduce With quicksight-gen** — the one-shot `audit verify report.pdf -c config.yaml --l2 <path>` command + a brief explanation of what it does.
  - **(c) Reproduce Manually** — per-source breakdown table (Transactions / Daily balances / L2 YAML / quicksight-gen code) showing each identifier + SHA, plus the **composite fingerprint row**, plus a ~30-line copy-pasteable Python recipe (canonical_value + hash_table + SHA256 over labeled lines) that recomputes the composite without quicksight-gen installed. Targets the regulator who wants to audit the auditor.
  - **L2 YAML byte-exact attachment**: `_attach_l2_yaml_to_pdf` runs after `multiBuild` and before pyHanko signing — pypdf opens the PDF, calls `PdfWriter.add_attachment(<l2_filename>, yaml_bytes)`, writes back. Verifier opens the PDF in any reader (Adobe / Preview / Foxit), grabs the file from the attachments panel, runs `sha256sum` to confirm it matches the embedded `l2_yaml_sha`. Closes the loop: a verifier can independently reproduce the L2 hash without needing the operator's filesystem.
  - **Notes / Exceptions box → fillable AcroForm text field**: `_make_fillable_notes_field` builds a `Flowable` subclass whose `draw` calls `canvas.acroForm.textfield(name="QSGNotesField", multiline, forceBorder)`. pyHanko's incremental signing leaves form fields unlocked by default, so the system seal applied first does NOT prevent the next reviewer from filling the field. When the reviewer signs, they decide whether to lock the field at that point.
  - **Tests: 20 → 23**: `test_audit_apply_pdf_embeds_l2_yaml_attachment` (attachment bytes hash equals on-disk YAML), `test_audit_apply_pdf_appendix_bookmarked_at_level_0` (sidebar nav entry), `test_audit_apply_pdf_notes_field_is_fillable_acroform` (real AcroForm field, not styled cell). Live verified on sasquatch_pr: PDF carries 1 attachment (`sasquatch_pr.yaml`), recomputed SHA matches `l2_yaml_sha` exactly, `audit verify` still green.
- [ ] **U.8 — Test layer.** Re-scoped 2026-05-02 to be **goal-oriented around U.8.b** (the credibility-contract release gate). U.8.a is the prerequisite that makes U.8.b possible without hardcoding; U.8.c is the cheap-CI guard rail that catches regressions before the gated job runs; U.8.d wires the `audit test` CLI verb.

  **Pinned principle: derive expected values from the scenario / test-data generator, never hardcode.** `len(scenario.drift_plants) == 3` rots the moment a seed-densification factor changes; `len(scenario.drift_plants)` against the same scenario is self-updating. The same `ScenarioPlant` that planted the rows is the source of truth for what the audit + dashboard should report.

- [x] **U.8.a — Scenario-expectations module (prerequisite for U.8.b).** New `tests/audit/_scenario_expectations.py` (the leading underscore + tests-dir location signal "test infra, not shipped surface"). Public API: `expected_audit_counts(scenario: ScenarioPlant, period: tuple[date, date]) -> ExpectedAuditCounts` — a frozen dataclass with one field per L1 invariant (`drift_count, overdraft_count, limit_breach_count, stuck_pending_count, stuck_unbundled_count, supersession_count`) plus per-invariant `expected_account_ids`/`expected_business_days` tuples for the row-identity asserts U.8.b.4 will need.
  - Time-series invariants (drift / overdraft / limit_breach / supersession-period-aggregate) filter `scenario.X_plants` by `days_ago` against the period bounds.
  - Current-state matview invariants (stuck_pending / stuck_unbundled) skip the period filter — plants are designed to never resolve, so `len(plants)` is the count regardless of when they were planted.
  - Supersession's pair-of-rows shape (original tx + corrected tx + matching daily_balance pair) handled by the helper, not the test caller.
  - **No live DB or browser needed** — pure-Python, runs anywhere. Lives next to the existing `tests/audit/test_*` so future audit tests can import it.
  - Tests: a unit suite that round-trips `default_scenario_for(spec_example)` through `expected_audit_counts(...)` and asserts the per-invariant numbers match a hand-counted golden (golden = walk the scenario tuples by inspection, NOT a hardcoded magic number).

- [ ] **U.8.b — PDF/dashboard agreement (release gate, v8.2.0 blocker).** The credibility contract: every number in the audit PDF MUST agree with what the deployed L1 dashboard shows AND what the scenario primitives planted, for the same period + L2 + DB snapshot. Auditor seeing one number while the operator sees another destroys trust in the entire generator; test plants vs auditor diverging is the same failure on the producer side. Three-way assertion per invariant: **expected count (from U.8.a) == PDF count (extracted from rendered cells) == dashboard count (counted via Playwright)** — three-way (not two-way) so a failure tells you which side broke. Decomposed for review-ability:
  - [x] **U.8.b.1 — PDF cell extractor.** New `tests/audit/_pdf_extract.py` exposing `count_invariant_table_rows(pdf_path, invariant: Literal[...]) -> int`. Walks the level-0 outline to locate each section's pages, layout-mode-extracts text, then counts "data row signature" lines: ≥ 3 columns of content (split on runs of ≥ 3 spaces) AND at least one numeric data signal (date / dollar / age suffix / trailing integer). This excludes header rows + wrapped-cell continuations + sub-section H3 headings ("Parent Accounts (Per-Row Detail)" / "Child Accounts Grouped by …" / "Aggregate (Entire Dataset)"). Counts across all sub-tables in the section (drift = single table; overdraft / stuck_unbundled = parent + child grouped; limit_breach / stuck_pending = child grouped only; supersession = aggregate + transactions detail). 17 tests cover the row/header/continuation discriminator (`_is_data_row`), empty-marker short-circuiting, mixed sub-tables with continuations, integration against a skeleton-mode PDF (all 6 invariants → 0), and unknown-section ValueError. Live verification on sasquatch_pr-with-DB PDF: drift=2, overdraft=18, limit_breach=2, stuck_pending=1, stuck_unbundled=3, supersession=2 (eyeballed against the rendered PDF; matches). Row-identity `extract_invariant_table_rows(...) -> list[dict]` deferred to U.8.b.4 when fanout needs it.
  - [x] **U.8.b.2 — Dashboard sheet row counter.** New `tests/audit/_dashboard_extract.py::count_l1_invariant_rows(page, invariant, period) -> int`. Per-invariant `_DASHBOARD_LAYOUT` map of `(sheet_name, table_visual_title, has_date_filter)` — drift→`Drift`/`Leaf Account Drift`, overdraft→`Overdraft`/`Overdraft Violations`, limit_breach→`Limit Breach`/`Limit Breach Detail`, stuck_pending→`Pending Aging`/`Stuck Pending Detail`, stuck_unbundled→`Unbundled Aging`/`Stuck Unbundled Detail`, supersession→`Supersession Audit`/`Logical Keys with Supersession`. For time-series invariants, applies the period via `set_parameter_datetime_value("Date From"/"Date To", "%Y/%m/%d")` (M.2b.1's universal date-filter shape); current-state invariants ignore the period. Counts via existing `count_table_total_rows` (handles QS vertical virtualization). 14 unit tests walk the live L1 dashboard tree and confirm every layout entry's (sheet, visual title) actually exists on the L1 app — catches typos / sheet renames at test-collect time without spinning up Playwright. Full integration (open dashboard, apply filter, count rows) lands in U.8.b.3.
  - **U.8.b.3 — Drift-only end-to-end (single-invariant smoke).** The wiring lap. Conftest fixture seeds DB via raw `default_scenario_for(l2_instance)` (NOT the demo `_apply_demo` densification path — predictability beats realism here), applies schema + seed + matview refresh, renders audit PDF, deploys L1 dashboard via existing `tests/e2e/_harness_*` pattern. Single test `test_audit_dashboard_agreement_drift` runs the three-way assert. Locks the wiring before fanout.
  - **U.8.b.4 — Fanout to remaining invariants.** One per commit so a regression in one doesn't block the whole sequence: overdraft → limit_breach → stuck_pending → stuck_unbundled → supersession. Each test is ~10 lines once U.8.b.3's fixtures land. Supersession's pair-of-rows shape gets a per-test note; the helper from U.8.a does the count math.
  - **U.8.b.5 — Both-dialects matrix.** Parametrize the conftest fixture over `["postgres", "oracle"]`. Gated behind `QS_GEN_E2E=1` per existing convention; CI matrix runs both cells in parallel. **This is the v8.2.0 release gate** — failure means rebuild before tag.
  - **Exec-summary aggregate metrics deferred** (transaction count / transfer count / dollar volume gross+net). These aren't cleanly derivable from plant tuples (they're baseline + plants summed) so the "derive from scenario" rule doesn't apply. PDF↔dashboard agreement on these can land later as a straight A==B comparison without the expected-from-scenario third leg.
  - **Out of scope: matview-evidence sidecar agreement.** The matview SHA256s in the appendix bind to the matview state at audit time; the dashboard reads the same matviews so they trivially agree. No assert needed.

- [ ] **U.8.c — Supporting CI tests (DB available, no browser).** CI has DB access (Postgres + Oracle) but no QuickSight / Playwright. So U.8.c covers everything short of the browser, which is most of U.8.b's value without the deploy-and-render cost. Two layers:
  - **No-DB layer:**
    - `tests/audit/test_sql.py` — locked SQL snapshot per invariant query, per dialect (dataset-contract pattern). Failure = unintentional SQL change.
    - `tests/audit/test_template_input.py` — assert the per-section dataclasses passed into reportlab carry the expected fields and shapes against a known fixture (same `default_scenario_for(spec_example)` source as U.8.a, so they stay aligned).
    - Existing `tests/audit/test_cli_smoke.py` keeps its structural smoke role.
  - **DB layer (the U.8.b-minus-dashboard test):** [x] Done 2026-05-02 (commit `<TBD>`).
    - `tests/audit/test_pdf_matches_scenario.py` — for each L1 invariant, seed DB via `default_scenario_for(l2_instance)`, render audit PDF, assert PDF section row count `>=` scenario-derived expected count. Both dialects verified (Postgres + Oracle, all 7 tests green).
    - **Inclusion (`>=`) rather than equality (`==`):** cross-plant interactions and multi-sub-table sections legitimately produce more PDF rows than there are plants — an overdraft plant's negative daily_balance with no offsetting postings ALSO surfaces in the drift matview (1 plant → 2 sections); a supersession plant renders in both the aggregate AND in-period detail sub-tables (1 plant → 2 PDF rows in section). Exact-count agreement is U.8.b's three-way contract over row IDENTITIES, which lives in U.8.b.4.
    - **`_TODAY = date.today()`** rather than the harness's pinned `date(2030, 1, 1)`: stuck_pending / stuck_unbundled matviews compute age via `CURRENT_TIMESTAMP - posting`, so plants pinned to a far-future date land in the SQL future relative to NOW and can't satisfy `age > max_pending_age_seconds`. Anchoring on real today keeps plant postings in the past where the matview can see them; days-ago offsets stay deterministic.
    - **Gating:** new `QS_GEN_DB_TESTS=1` env var (separate from `QS_GEN_E2E=1` since it's destructive-DB-but-no-browser). Skips when unset OR when no `demo_database_url` is configured.
    - Catches the producer-side half of U.8.b's three-way contract: if the SQL or matview pipeline drifts from what the scenario planted, this test fails before U.8.b's gated job ever runs. U.8.b then only adds the dashboard leg.

- [ ] **U.8.d — `audit test` CLI subcommand.** Mirrors the four-artifact convention (`schema test` / `data test` / `json test` / `docs test`). Runs U.8.a/c locally without external infra; defers to `QS_GEN_E2E=1` for the U.8.b matrix. Wraps pytest + pyright like the other groups.
- [x] **U.9 — CLI surface tests.** `--help` smoke + emit-only path against `spec_example`. Mirror the pattern queued for the other groups in the Backlog.
  - ~~**Per-command --help smoke tests** for the new artifact groups. `schema apply` / `data apply` / `data refresh` / `data clean` / `json clean` / `docs apply` / `docs serve` aren't directly exercised by unit tests today — the integration job covers the `--execute` paths against real DB, and emit-only paths flow through pre-existing dataset-shape tests. Add a `tests/{schema,data,json,docs}/test_cli_smoke.py` that asserts `--help` exits 0 + the emit path against `spec_example` produces a non-empty SQL stream.~~ Done 2026-05-02. `tests/{schema,data,json,docs}/test_cli_smoke.py` land 34 cases between them: per-group `--help` lists every subcommand; per-verb `--help` exits 0; emit-only paths exercised for `schema apply`/`clean`, `data apply`/`refresh`/`clean`/`etl-example`, `json apply` (writes four-app bundle to tmp), `docs clean` (no-op + remove existing). `json clean` and `json probe` get `--help`-only smoke (need AWS / deployed dashboards to advance further); `docs apply`/`serve` and every group's `test` verb get `--help`-only smoke (subprocess wrappers around mkdocs / pytest / pyright that recurse if invoked from a pytest run).
- [ ] **U.10 — Documentation.**
  - `docs/handbook/audit.md` — reference page covering: what the report contains, how to generate it, how to verify the fingerprint, how to apply a cryptographic signature externally.
    - Also needs to detail and provide certificate creation instructions.
  - Update `docs/for-your-role/compliance-analyst.md` with the audit-report onramp.
    - probably needs integrator language that the report should be automatically generated daily
  - README + CLAUDE.md mention the new artifact group.
- [ ] **U.11 — Cut release.** v8.2.0 — additive (new artifact group + new optional `reportlab` dep). RELEASE_NOTES entry. Tag, push, verify pipeline.

---
# Proposed Phase V
- Convert to using uv from pip
- Today's split between `run/config.yaml` (account, region, datasource, dialect, theme defaults) and the L2 institution YAML (rails, chains, accounts, persona, theme override) has accumulated friction points. Audit + tighten the boundary based on what actually got threaded in M-/N-/O-/P-/Q-. Defers to a dedicated phase with its own scoping pass after Q.3.a settles.
  - Comments: I think there is a natural split between insitution and environment.
    - config.yaml has 
      - aws account
      - aws region
      - tag prefix
      - quicksight datasource (optional but if left out, json generates it)
      - database dialect, 
      - database connection (optional but required for demo data automatic population and audit pdf)
      - signing key (optional but enables strong pdf signatures for audit trail)
    - institution has
      - rails
      - chains
      - accounts
      - persona
      - theme override (defaults live in the code)
- ~~**Vendor `@hpcc-js/wasm-graphviz` for offline-friendly docs** (was T.7). `qs-graphviz-wasm.js` currently CDN-loads from jsDelivr.~~ Done 2026-05-02 (commit `2328cea`). Vendored v1.21.5 to `src/quicksight_gen/docs/stylesheets/wasm-graphviz/index.js` (800KB ESM with WASM inlined as base64); loader imports relative to it; mkdocs `--strict` build green.
- **Re-run 4-cell e2e matrix (P.9f.d)** — was deferred when the per-cell triage list was still settling. Worth a green pass against v8.0.0 once any first-impression tune-ups in R.6.e land.
---

## Phase R — Realistic demo seed (open follow-ups)

Phase R landed v7.2.0 (3-month healthy baseline + embedded plants). Three open follow-ups remain:

- [ ] **R.6.e — "First impression" tune-up.** Two known tuning targets where baseline data still surfaces "real bookkeeping cascade" signal as L1 invariant violations. Both need invariant-aware leg-loop work that's out of scope for first land:
  - **Overdraft on intermediate clearing accounts** (~220 rows on ach_orig_settlement, merchant_payable_clearing, internal_transfer_suspense, ZBA sub-accounts). Cause: baseline emits transfers in random order, so causal cascades (customer outbound → settlement → ZBA sweep → concentration → FRB) don't preserve cause-before-effect timing. Intermediate accounts swing into negative as a result. Fix options: (a) restructure leg loop to enforce causal ordering, (b) materialize zero-net intermediate-clearing legs per cascade per day, (c) widen account starting-balance cushions further.
  - **Limit_breach on customer outbound** (~56 rows). Cause: amount sampler clamps each transfer to the LimitSchedule cap individually, but daily aggregate across multiple firings can exceed cap. Fix: track per-(account, transfer_type, day) cumulative outbound during emission and clamp incremental amounts.
- [ ] **R.7.d — Re-screenshot at 1280×900 against the v7.2.0+ demo.** Re-run `quicksight-gen docs screenshot --all -o src/quicksight_gen/docs/_screenshots/` against the deployed sasquatch_pr dashboards once the dashboards are stable post-v8.0.0. Current screenshots predate Phase R's realistic baseline.
- [ ] **R.7.e — Lift R.1.f spec out of PLAN_ARCHIVE.md into a docs-site reference page.** Once the implementation has stabilized + the headline numbers in the spec match what the generator actually produces, lift the design doc into the docs site as durable reference. Likely target: `docs/handbook/seed-generator.md`.

---

## Backlog

Single grab-bag for everything not yet in a phase. Promote to a numbered phase entry when work starts.

### L2 model gaps

- **Validator: every Transfer MUST match a Rail.**
- **Validator: no overlap on (role, transfer_type) entries.**
- **Validator: LimitSchedule uniqueness on (parent_role, transfer_type).**
- **Multiple dashboards from one L2 instance** (shared prefix + naming).
- **PR dashboard → generic L2-validation dashboard** (re-skinning of L2FT for a different validation persona).
- **Lift seed primitives into `common/l2/seed.py`** (was M.2d.5).

### Tooling / test reliability

- **Single-app deploy must not orphan shared datasource.** (Mostly moot under v8.0.0 since `json apply` emits all four; keep as defensive note.)
- **Apply layered (query+render) pattern to all browser e2e tests** (was M.4.1.k).
- **Sasquatch L1 dashboard render flake.** `test_harness_l1_planted_scenarios_visible[sasquatch_pr]` Layer 2 occasionally misses `cust-0001-snb` on the Limit Breach sheet — Layer 1 (matview row presence) passes, the row IS in the matview, but the deployed Limit Breach table doesn't render the cell within the visual timeout. One retry already baked in via `run_dashboard_check_with_retry`; second attempt also misses. Spec_example + fuzz variants of the same test pass on the same run, so the flake is data-shape-specific (sasquatch_pr's seed has more transactions; the L1 dashboard's per-sheet transfer_type dropdown may default-narrow before the table loads). Investigation work: add a screenshot comparison of the Limit Breach sheet between sasquatch_pr (failing) and spec_example (passing) at the moment of the assertion, OR widen the harness's per-sheet wait to assert "table rendered" before sheet_text capture, OR re-deploy sasquatch_pr seed with a tighter days_ago=1 limit_breach plant to rule out timing. Do NOT xfail (the M.4.4.12 lesson — silent xfails masked real bugs).
- **L1 dashboard date filter doesn't surface matview rows** (root cause TBD — non-blocking workaround in place).

- **QS-UI kitchen-sink reference tool** (was M.4.4.9). Standalone tool that consumes QS console "view JSON" output for every visual type and dumps it as a reference fixture. Defensive measure; deferred since the concrete editor-crash bugs got fixed.


### Dashboard polish — Q.1.c follow-ups

- **Q.1.a.3 — Auto-derive plain-English axis labels for BarChart** (replace raw column names like `transfer_type` with `Transfer Type`). Manual labels landed on the most visible chart (L1 Today's Exceptions) via Q.1.c; auto-derivation is the broader sweep.
- **Executives Transaction Volume + Money Moved — metadata grouping** (was Q.1.c.6). Needs L2-instance-aware metadata key dropdowns (cascading Key + Value like L2FT Rails sheet) plus a dataset pivot to expose metadata as a dim. Bigger than a punch-list item; queue as its own sub-phase.

### Audit / data evaluation / app info

- **Postgres dataset evaluator** — given a connection, evaluate whether all exception cases are present; report stats on the CLI.
- **App Info sheet enhancements** — version of `quicksight-gen` used to generate (so version mismatches are detectable); most-recent `<prefix>_transactions` / `<prefix>_daily_balances` row date (so ETL can be troubleshooted); most-recent matview refresh timestamp.

### Tech debt

- **Encode more invariants in the type system.** K.2 did this for drill-param shape compatibility; Phase L's tree primitives close another big chunk. What remains after L is the candidate list for the next round.
- **Drop `tests/json/test_l2_flow_tracing_matrix.py`'s implicit dependency on `cli` module imports** if any survived the Q.3.a reorg.

### Known platform limitations — do not re-attempt without new evidence

- **QS URL-parameter control sync** — K.4.7 cross-app drills dropped. URL fragment sets the parameter store but doesn't push values into bound controls. Re-entry conditions: AWS fix, custom embedded app via `setParameters()` SDK, or a new URL form that triggers control sync. See `PLAN_ARCHIVE.md` for full re-entry details.
- **QS dropdown click target is the middle grey bar** — `ParameterDropDownControl` only opens on the inner grey bar; clicking the visible edge does nothing. Suggest before investigating "unresponsive dropdown" reports.
- **QS silent-fail mode** — datasets healthy + describe-cleanly, every visual on every sheet shows the spinner forever. See CLAUDE.md → Operational Footguns for the diagnostic ladder.
